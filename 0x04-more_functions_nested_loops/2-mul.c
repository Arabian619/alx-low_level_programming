#include "main.h"
/**
 * mul - Looks for a digit (0 through 9).
 * @a: a - Variable
 * @b: b - variable
 * Return: return 0 always (Success)
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}

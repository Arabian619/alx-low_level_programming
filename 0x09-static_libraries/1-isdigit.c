#include "main.h"

/**
 * _isdigit - Looks for a digit (0 through 9)
 * @c: int to look for
 * Return: If c is a digit return 1, Otherwise 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

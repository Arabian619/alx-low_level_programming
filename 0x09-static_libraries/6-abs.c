#include "main.h"

/**
 * _abs - Calculates absolute value of an integer.
 * @n: Integer Input.
 *
 * Return: Returns absolute value of n.
 */

	int _abs(int n)
	{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
	}
